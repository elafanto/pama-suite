/** @type {import('@capacitor/cli').CapacitorConfig} */
const config = {
  appId: 'in.pama.businesssuite',
  appName: 'Pama Business Suite',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
}

export default config
